NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Guest emails (planting confirmation, quarterly updates). Get a key at
# resend.com — without it, emails are silently skipped (logged, not sent),
# everything else keeps working. RESEND_FROM_EMAIL is optional; defaults to
# Resend's shared test address until a real domain is verified.
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL="Grovi <onboarding@resend.dev>"
